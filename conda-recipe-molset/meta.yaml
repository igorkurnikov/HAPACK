{% set version = "22.08.06" %}

package:
  name: molset
  version: {{ version }}

source:
  path: ../../MOLSET_BIN_LIBS
  
requirements:
  build:
    - python
    - setuptools
    
  run:
    - python
    - pip    

