{% set version = "23.10.08" %}

package:
  name: molset
  version: {{ version }}

source:
  path: ../../MOLSET_BIN_LIBS
  
requirements:
  build:
    - python
    - setuptools
    
  run:
    - python
    - pip    

