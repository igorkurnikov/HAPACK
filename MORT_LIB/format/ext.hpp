#include <common.hpp>

namespace mort
{

    bool is_dbfmat( const hashid_t& format );

    string get_ext( const string& file );

    hashid_t get_fmt( const string& ext );

} // namespace mort
        
